name: kivuli_app
description: "Mfumo wa Kivuli Radar na Kivuli cha Ndoto"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Hizi ndizo ulizokuwa nazo kwenye picha (10404.jpg)
  provider: ^6.0.5
  http: ^1.1.0
  agora_rtc_engine: ^6.2.0
  permission_handler: ^11.0.1
  firebase_core: ^2.15.0
  cloud_functions: ^4.3.0
  firebase_core: ^2.0.0 
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  # Kama una picha za radar au logo, utaziweka hapa chini baadae
  # assets:
  #   - assets/images/
  
